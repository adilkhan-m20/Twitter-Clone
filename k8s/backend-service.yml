apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: twitter-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP # ClusterIP means this is strictly internal. The outside world cannot hit your API directly.
