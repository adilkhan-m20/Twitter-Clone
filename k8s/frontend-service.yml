apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: twitter-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer # This tells the cloud provider to give you a public IP address!
